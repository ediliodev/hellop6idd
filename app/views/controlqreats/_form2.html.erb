<%= form_for(controlqreat) do |f| %>
  <% if controlqreat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controlqreat.errors.count, "error") %> prohibited this controlqreat from being saved:</h2>

      <ul>
      <% controlqreat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %>
    <%= f.number_field :numero, :readonly => true %>
  </div>

  <div class="field">
    <%= f.label :limite %>
    <%= f.number_field :limite %>
  </div>

  <div class="field">
    <%= f.label :vendida %>
    <%= f.number_field :vendida, :readonly => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
