<%= form_for(monedat) do |f| %>
  <% if monedat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(monedat.errors.count, "error") %> prohibited this monedat from being saved:</h2>

      <ul>
      <% monedat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :simbolo %>
    <%= f.text_field :simbolo %>
  </div>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :paist_id %>
    <%= f.select :paist_id, Paist.order(:nombre).collect{ |item|  [ " --> " + item.nombre , item.id ] } %>     

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
