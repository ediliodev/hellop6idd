<p id="notice"><%= notice %></p>

<h2>Control y Monitoreo de Pale</h2>
<h2>Nacional Noche</h2>


<table>
  <thead>
    <tr>
     
      <th>Pale:</th>
      <th>Vendido</th>
       <th>Limite</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @controlpnntsb.each do |controlpnnt| %>
      <tr>
        
        <td><b><%= controlpnnt.siglas %></td>
        <td>$<%= controlpnnt.vendida %></td>
        <td>$<%= controlpnnt.limite %></td>
        <td><%= link_to 'Show', controlpnnt %></td>
        <td><%= link_to 'Edit', edit_controlpnnt_path(controlpnnt) %></td>
        <td><%= link_to 'Destroy', controlpnnt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h2>Ordenado por pales:</h2>


<table>
  <thead>
    <tr>
     
      <th>Pale</th>
      <th>Vendido</th>
      <th>Limite</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @controlpnnts.each do |controlpnnt| %>
      <tr>
        
        <td><b><%= controlpnnt.siglas %></td>
        <td>$<%= controlpnnt.vendida %></td>
        <td>$<%= controlpnnt.limite %></td>
        <td><%= link_to 'Show', controlpnnt %></td>
        <td><%= link_to 'Edit', edit_controlpnnt_path(controlpnnt) %></td>
        <td><%= link_to 'Destroy', controlpnnt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

