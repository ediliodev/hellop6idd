<p id="notice"><%= notice %></p>


<h1>Reporte de Resultados</h1>
<h2>Desde:  <%= @dia1.to_s %> </h2>
<h2>Hasta:  <%= @dia2.to_s %> </h2>


<table>
  <thead>
    <tr>
      <th>Sucursal</th>
      <th>Ventas</th>
      <th>Ganadores</th>
      <th>PendxPagar</th>
      <th>Balance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @menuadmrresultadoglobalts.each do |menuadmrresultadoglobalt| %>
      <tr>
       
        <td><%=menuadmrresultadoglobalt.sucursal %></td>
        
        <td><%=number_to_currency(menuadmrresultadoglobalt.venta, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>
        
        <td><%= number_to_currency(menuadmrresultadoglobalt.ganadores, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>

        <td><%= number_to_currency(menuadmrresultadoglobalt.pendientexpagar, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>

        <td><%= number_to_currency(menuadmrresultadoglobalt.balance, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>
       
      </tr>
    <% end %>

     <tr> 
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
       </tr>

       <tr> 
          <td><b>T-> <%= @total_sucursales %></td>
          <td><b><%= number_to_currency(@total_ventas, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>
          <td><b><%= number_to_currency(@total_ganadores, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>

          <td><b><%= number_to_currency(@total_pendientexpagar, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>

          <td><b><%= number_to_currency(@total_balance, :unit => "$", :delimiter => ",", :precision => 0, :separator => ".") %></td>

       </tr>

  </tbody>
</table>

<br>

<%= link_to 'Nueva Consulta', new_menuadmrresultadoglobalt_path %>
<br>
<%= link_to 'Menu', menuts_path %>

