<%= form_for(paist) do |f| %>
  <% if paist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paist.errors.count, "error") %> prohibited this paist from being saved:</h2>

      <ul>
      <% paist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :continentet_id %>
    <%= f.select :continentet_id, Continentet.order(:nombre).collect{ |item|  [ " --> " + item.nombre , item.id ] } %>     
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
