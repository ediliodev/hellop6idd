<%= form_for(ciudadt) do |f| %>
  <% if ciudadt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ciudadt.errors.count, "error") %> prohibited this ciudadt from being saved:</h2>

      <ul>
      <% ciudadt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :provinciat_id %>
    <%= f.select :provinciat_id, Provinciat.order(:nombre).collect{ |item|  [ item.nombre , item.id ] } %>     
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
