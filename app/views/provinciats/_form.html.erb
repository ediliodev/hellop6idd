<%= form_for(provinciat) do |f| %>
  <% if provinciat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provinciat.errors.count, "error") %> prohibited this provinciat from being saved:</h2>

      <ul>
      <% provinciat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :paist_id %>
    <%= f.select :paist_id, Paist.order(:nombre).collect{ |item|  [ " --> " +  item.nombre , item.id ] } %>     
  </div>


  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
