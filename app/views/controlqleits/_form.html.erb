<%= form_for(controlqleit) do |f| %>
  <% if controlqleit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controlqleit.errors.count, "error") %> prohibited this controlqleit from being saved:</h2>

      <ul>
      <% controlqleit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %>
    <%= f.number_field :numero %>
  </div>

  <div class="field">
    <%= f.label :limite %>
    <%= f.number_field :limite %>
  </div>

  <div class="field">
    <%= f.label :vendida %>
    <%= f.number_field :vendida %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
