<%= form_for(mensajepiedepaginat) do |f| %>
  <% if mensajepiedepaginat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mensajepiedepaginat.errors.count, "error") %> prohibited this mensajepiedepaginat from being saved:</h2>

      <ul>
      <% mensajepiedepaginat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipodemensaje %>
    <%= f.text_field :tipodemensaje %>
  </div>

  <div class="field">
    <%= f.label :mensaje %>
    <%= f.text_field :mensaje %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
